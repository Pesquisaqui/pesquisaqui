<p id="notice"><%= notice %></p>

<%= @user.name %>

<ul>
  <% @user.topics.each do |topic| %>
    <li><%= topic.name %></li>
  <% end %>
<ul>

<p>Quantidade de publicações: <%= @user.publication_count %></p>

<%= link_to 'Back', users_path %>
